name: Jood IPA Web App

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    timeout-minutes: 60

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Select latest installed Xcode
        shell: bash
        run: |
          set -euo pipefail

          XCODE_APP="$(find /Applications -maxdepth 1 -type d -name 'Xcode*.app' | sort -V | tail -n 1)"

          if [ -z "${XCODE_APP}" ]; then
            echo "No Xcode installation was found."
            exit 1
          fi

          sudo xcode-select --switch "${XCODE_APP}/Contents/Developer"

          echo "Selected Xcode:"
          xcodebuild -version
          echo "Swift:"
          swift --version

      - name: Install XcodeGen and zip
        shell: bash
        run: |
          set -euo pipefail
          brew install xcodegen
          command -v xcodegen
          command -v zip

      - name: Create clean project
        shell: bash
        run: |
          set -euo pipefail
          rm -rf JoodIPA
          mkdir -p JoodIPA/JoodIPA

      - name: Generate Info.plist
        shell: bash
        run: |
          cat > JoodIPA/JoodIPA/Info.plist <<'PLIST'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
            "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleDisplayName</key>
              <string>Jood IPA</string>

              <key>CFBundleIdentifier</key>
              <string>com.jood.ipa</string>

              <key>CFBundleName</key>
              <string>Jood IPA</string>

              <key>CFBundlePackageType</key>
              <string>APPL</string>

              <key>CFBundleShortVersionString</key>
              <string>1.0</string>

              <key>CFBundleVersion</key>
              <string>1</string>

              <key>UILaunchScreen</key>
              <dict>
                  <key>UIColorName</key>
                  <string>LaunchBackground</string>
              </dict>

              <key>UIRequiresFullScreen</key>
              <false/>

              <key>UISupportedInterfaceOrientations</key>
              <array>
                  <string>UIInterfaceOrientationPortrait</string>
                  <string>UIInterfaceOrientationPortraitUpsideDown</string>
                  <string>UIInterfaceOrientationLandscapeLeft</string>
                  <string>UIInterfaceOrientationLandscapeRight</string>
              </array>

              <key>UISupportedInterfaceOrientations~ipad</key>
              <array>
                  <string>UIInterfaceOrientationPortrait</string>
                  <string>UIInterfaceOrientationPortraitUpsideDown</string>
                  <string>UIInterfaceOrientationLandscapeLeft</string>
                  <string>UIInterfaceOrientationLandscapeRight</string>
              </array>

              <key>CFBundleIcons</key>
              <dict>
                  <key>CFBundlePrimaryIcon</key>
                  <dict>
                      <key>CFBundleIconFiles</key>
                      <array>
                          <string>AppIcon60x60@2x.png</string>
                      </array>
                      <key>CFBundleIconName</key>
                      <string>AppIcon</string>
                  </dict>
              </dict>

              <key>CFBundleIcons~ipad</key>
              <dict>
                  <key>CFBundlePrimaryIcon</key>
                  <dict>
                      <key>CFBundleIconFiles</key>
                      <array>
                          <string>AppIcon76x76@2x~ipad.png</string>
                      </array>
                      <key>CFBundleIconName</key>
                      <string>AppIcon</string>
                  </dict>
              </dict>

              <key>NSAppTransportSecurity</key>
              <dict>
                  <key>NSAllowsArbitraryLoads</key>
                  <false/>
              </dict>
          </dict>
          </plist>
          PLIST

      - name: Generate Swift app
        shell: bash
        run: |
          cat > JoodIPA/JoodIPA/JoodIPAApp.swift <<'SWIFT'
          import SwiftUI

          @main
          struct JoodIPAApp: App {
              var body: some Scene {
                  WindowGroup {
                      WebContainerView()
                          .ignoresSafeArea()
                  }
              }
          }
          SWIFT

      - name: Generate WebView
        shell: bash
        run: |
          cat > JoodIPA/JoodIPA/WebContainerView.swift <<'SWIFT'
          import SwiftUI
          import WebKit

          private let joodURL = URL(string: "https://jood-ipa.neocities.org")!

          struct WebContainerView: UIViewRepresentable {
              func makeCoordinator() -> Coordinator {
                  Coordinator()
              }

              func makeUIView(context: Context) -> WKWebView {
                  let configuration = WKWebViewConfiguration()
                  configuration.allowsInlineMediaPlayback = true
                  configuration.mediaTypesRequiringUserActionForPlayback = []

                  let preferences = WKPreferences()
                  preferences.javaScriptCanOpenWindowsAutomatically = true
                  configuration.preferences = preferences

                  let webView = WKWebView(frame: .zero, configuration: configuration)

                  webView.navigationDelegate = context.coordinator
                  webView.uiDelegate = context.coordinator
                  webView.allowsBackForwardNavigationGestures = true
                  webView.scrollView.contentInsetAdjustmentBehavior = .never
                  webView.scrollView.alwaysBounceVertical = false
                  webView.scrollView.alwaysBounceHorizontal = false
                  webView.backgroundColor = .systemBackground
                  webView.isOpaque = true

                  webView.load(URLRequest(
                      url: joodURL,
                      cachePolicy: .useProtocolCachePolicy,
                      timeoutInterval: 30
                  ))

                  return webView
              }

              func updateUIView(_ webView: WKWebView, context: Context) {
                  // The app intentionally contains no browser address bar,
                  // toolbar, tabs, or Safari UI.
              }

              final class Coordinator: NSObject, WKNavigationDelegate, WKUIDelegate {
                  func webView(
                      _ webView: WKWebView,
                      decidePolicyFor navigationAction: WKNavigationAction,
                      decisionHandler: @escaping (WKNavigationActionPolicy) -> Void
                  ) {
                      decisionHandler(.allow)
                  }

                  func webView(
                      _ webView: WKWebView,
                      createWebViewWith configuration: WKWebViewConfiguration,
                      for navigationAction: WKNavigationAction,
                      windowFeatures: WKWindowFeatures
                  ) -> WKWebView? {
                      // Open target=_blank links inside the same app view.
                      webView.load(navigationAction.request)
                      return nil
                  }
              }
          }
          SWIFT


      - name: Prepare AppIcon assets
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p JoodIPA/JoodIPA/Assets.xcassets/AppIcon.appiconset

          if [ ! -f "AppIcon60x60@2x.png" ]; then
            echo "Missing AppIcon60x60@2x.png in repository root."
            exit 1
          fi

          if [ ! -f "AppIcon76x76@2x~ipad.png" ]; then
            echo "Missing AppIcon76x76@2x~ipad.png in repository root."
            exit 1
          fi

          cp "AppIcon60x60@2x.png" \
            "JoodIPA/JoodIPA/Assets.xcassets/AppIcon.appiconset/AppIcon60x60@2x.png"

          cp "AppIcon76x76@2x~ipad.png" \
            "JoodIPA/JoodIPA/Assets.xcassets/AppIcon.appiconset/AppIcon76x76@2x~ipad.png"

          cat > JoodIPA/JoodIPA/Assets.xcassets/Contents.json <<'JSON'
          {
            "info" : {
              "author" : "xcode",
              "version" : 1
            }
          }
          JSON

          cat > JoodIPA/JoodIPA/Assets.xcassets/AppIcon.appiconset/Contents.json <<'JSON'
          {
            "images" : [
              {
                "filename" : "AppIcon60x60@2x.png",
                "idiom" : "iphone",
                "scale" : "2x",
                "size" : "60x60"
              },
              {
                "filename" : "AppIcon76x76@2x~ipad.png",
                "idiom" : "ipad",
                "scale" : "2x",
                "size" : "76x76"
              }
            ],
            "info" : {
              "author" : "xcode",
              "version" : 1
            }
          }
          JSON

      - name: Generate XcodeGen project.yml
        shell: bash
        run: |
          cat > JoodIPA/project.yml <<'YAML'
          name: JoodIPA

          options:
            minimumXcodeGenVersion: "2.40.0"
            deploymentTarget:
              iOS: "17.0"
            createIntermediateGroups: true
            groupSortPosition: top

          settings:
            base:
              SWIFT_VERSION: "6.0"
              IPHONEOS_DEPLOYMENT_TARGET: "17.0"
              TARGETED_DEVICE_FAMILY: "1,2"
              ASSETCATALOG_COMPILER_INCLUDE_ALL_APPICON_ASSETS: "YES"
              SUPPORTS_XR_DESIGNED_FOR_IPHONE_IPAD: "YES"
              DEVELOPMENT_TEAM: ""
              CODE_SIGNING_ALLOWED: "NO"
              CODE_SIGNING_REQUIRED: "NO"
              CODE_SIGN_IDENTITY: ""
              PRODUCT_BUNDLE_IDENTIFIER: "com.jood.ipa"
              PRODUCT_NAME: "JoodIPA"
              MARKETING_VERSION: "1.0"
              CURRENT_PROJECT_VERSION: "1"

          targets:
            JoodIPA:
              type: application
              platform: iOS
              sources:
                - path: JoodIPA
              info:
                path: JoodIPA/Info.plist
              settings:
                base:
                  PRODUCT_NAME: "JoodIPA"
                  INFOPLIST_FILE: "JoodIPA/Info.plist"
                  ASSETCATALOG_COMPILER_APPICON_NAME: "AppIcon"
                  CODE_SIGNING_ALLOWED: "NO"
                  CODE_SIGNING_REQUIRED: "NO"
              scheme:
                gatherCoverageData: false
                testTargets: []
          YAML

      - name: Generate Xcode project
        shell: bash
        run: |
          set -euo pipefail
          cd JoodIPA
          xcodegen generate
          test -f JoodIPA.xcodeproj/project.pbxproj

      - name: Validate Swift sources
        shell: bash
        run: |
          set -euo pipefail
          cd JoodIPA
          swiftc -parse \
            JoodIPA/JoodIPAApp.swift \
            JoodIPA/WebContainerView.swift

      - name: Build unsigned archive
        shell: bash
        run: |
          set -euo pipefail

          cd JoodIPA

          xcodebuild \
            -project JoodIPA.xcodeproj \
            -scheme JoodIPA \
            -configuration Release \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            -archivePath "$RUNNER_TEMP/JoodIPA.xcarchive" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            DEVELOPMENT_TEAM="" \
            archive

      - name: Package unsigned IPA
        shell: bash
        run: |
          set -euo pipefail

          ARCHIVE="$RUNNER_TEMP/JoodIPA.xcarchive"
          APP_PATH="$ARCHIVE/Products/Applications/JoodIPA.app"

          if [ ! -d "$APP_PATH" ]; then
            echo "App bundle was not found:"
            find "$ARCHIVE" -maxdepth 5 -type d -name '*.app' -print
            exit 1
          fi

          rm -rf "$RUNNER_TEMP/Payload"
          mkdir -p "$RUNNER_TEMP/Payload"

          cp -R "$APP_PATH" "$RUNNER_TEMP/Payload/JoodIPA.app"

          cd "$RUNNER_TEMP"
          rm -f JoodIPA-unsigned.ipa
          /usr/bin/zip -qry JoodIPA-unsigned.ipa Payload

          mkdir -p "$GITHUB_WORKSPACE/artifacts"
          cp JoodIPA-unsigned.ipa "$GITHUB_WORKSPACE/artifacts/JoodIPA-unsigned.ipa"

      - name: Upload unsigned IPA
        uses: actions/upload-artifact@v4
        with:
          name: JoodIPA-unsigned-ipa
          path: artifacts/JoodIPA-unsigned.ipa
          if-no-files-found: error

      - name: Build summary
        shell: bash
        run: |
          {
            echo "## Jood IPA"
            echo ""
            echo "- Native iOS SwiftUI application"
            echo "- Displays https://jood-ipa.neocities.org directly inside the app"
            echo "- No Safari address bar, browser tabs, or browser toolbar"
            echo "- WKWebView fills the complete available iPhone/iPad screen"
            echo "- iPhone and iPad supported via TARGETED_DEVICE_FAMILY=1,2"
            echo "- Portrait and landscape orientations supported"
            echo "- Unsigned IPA: artifacts/JoodIPA-unsigned.ipa"
          } >> "$GITHUB_STEP_SUMMARY"
